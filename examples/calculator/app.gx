package main

func App() (Component) {
	stateChannel := make(chan CalculatorState, 10)
	stateChannel <- CalculatorState{
		Expression:     "",
		CurrentInput:   "",
		JustCalculated: false,
		LastResult:     0,
	}

	Div(Class("app-container")) {
		H1 {
			"Calculator"
		}
		Calculator(WithStateChannel(stateChannel))
		Div(Class("info")) {
			"Built with Guix"
		}
	}
}
