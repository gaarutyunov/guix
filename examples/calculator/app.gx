package main

func App() {
	stateChannel := make(chan CalculatorState, 10)
	_ = InitCalculatorState(stateChannel)

	Div(Class("app-container")) {
		H1 {
			"Calculator"
		}
		Calculator(WithStateChannel(stateChannel))
		Div(Class("info")) {
			"Built with Guix"
		}
	}
}
