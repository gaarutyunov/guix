{
  "name": "webgpu-cube-example",
  "version": "1.0.0",
  "description": "WebGPU rotating cube example for Guix",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
